{
  "name": "miracle-boost",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Адаптивный лендинг Miracle Boost с калькулятором FACEIT-бустинга, чатом поддержки и админ-панелью.",
  "scripts": {
    "dev": "node scripts/server.mjs",
    "start": "node scripts/server.mjs dist",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/server.mjs dist 4173",
    "db:schema": "node -e \"console.log(require('fs').readFileSync('database/schema.sql','utf8'))\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
